# Portainer EE 2.42.0 operation index — generated by `make specs`; do not edit.
# Format: METHOD /path → chunk file — summary
POST /api/auth → auth.yaml — Authenticate
POST /api/auth/logout → auth.yaml — Logout
POST /api/auth/oauth/validate → auth.yaml — Authenticate with OAuth
GET /api/auto_updates → auto_updates.yaml — List auto updates
POST /api/backup → backup.yaml — Creates an archive with a system data snapshot that could be used to restore the system.
POST /api/backup/azure/execute → backup.yaml — Execute an ad-hoc backup to Azure Blob Storage
POST /api/backup/azure/restore → backup.yaml — Restore system state from an Azure Blob Storage backup
GET /api/backup/azure/settings → backup.yaml — Fetch Azure Blob Storage backup settings
POST /api/backup/azure/settings → backup.yaml — Update Azure Blob Storage backup settings and reschedule cron job
GET /api/backup/azure/status → backup.yaml — Fetch the status of the last scheduled Azure Blob backup run
POST /api/backup/azure/test → backup.yaml — Test connectivity to an Azure Blob Storage container
POST /api/backup/local/run → backup.yaml — Trigger an immediate local backup outside of the cron schedule
GET /api/backup/local/settings → backup.yaml — Fetch local scheduled backup settings
POST /api/backup/local/settings → backup.yaml — Updates local scheduled backup settings and restarts the cron job as needed
GET /api/backup/local/status → backup.yaml — Fetch the status of the last scheduled local backup run
POST /api/backup/s3/execute → backup.yaml — Execute backup to AWS S3 Bucket
POST /api/backup/s3/restore → backup.yaml — Triggers a system restore using details of s3 backup
GET /api/backup/s3/settings → backup.yaml — Fetch s3 backup settings/configurations
POST /api/backup/s3/settings → backup.yaml — Updates stored s3 backup settings and updates running cron jobs as needed
GET /api/backup/s3/status → backup.yaml — Fetch the status of the last scheduled backup run
POST /api/cloud/amazon/provision → kaas.yaml — Provision a new Amazon EKS and create an environment
POST /api/cloud/azure/provision → kaas.yaml — Provision a new Microsoft Azure cluster and create an environment
GET /api/cloud/credentials → cloud_credentials.yaml — getAll cloud credentials
POST /api/cloud/credentials → cloud_credentials.yaml — Create a cloud credential
GET /api/cloud/credentials/{id} → cloud_credentials.yaml — getByID gets a cloud credential by ID
PUT /api/cloud/credentials/{id} → cloud_credentials.yaml — Update a cloud credential
DELETE /api/cloud/credentials/{id} → cloud_credentials.yaml — Delete a cloud credential
POST /api/cloud/endpoints/{environmentId}/upgrade → kaas.yaml — Upgrade the cluster to the next stable kubernetes version.
GET /api/cloud/gitcredentials → cloud_credentials.yaml — Get Shared Git Credentials
POST /api/cloud/gitcredentials → cloud_credentials.yaml — Create Shared Git Credential
GET /api/cloud/gitcredentials/{id} → cloud_credentials.yaml — Get Shared Git Credential
PUT /api/cloud/gitcredentials/{id} → cloud_credentials.yaml — Update a Shared Git Credential
DELETE /api/cloud/gitcredentials/{id} → cloud_credentials.yaml — Delete Shared Git Credential
POST /api/cloud/gke/provision → kaas.yaml — Provision a new Google Kubernetes (GKE) cluster and create an environment
GET /api/cloud/{provider}/info → kaas.yaml — Get information about the provisioning options for a cloud provider.
POST /api/cloud/{provider}/provision → kaas.yaml — Provision a new CIVO, Linode or Digital Ocean cluster and create an environment
GET /api/custom_templates → custom_templates.yaml — List available custom templates
POST /api/custom_templates → custom_templates.yaml — Create a custom template
POST /api/custom_templates/create/file → custom_templates.yaml — Create a custom template
POST /api/custom_templates/create/repository → custom_templates.yaml — Create a custom template
POST /api/custom_templates/create/string → custom_templates.yaml — Create a custom template
GET /api/custom_templates/{id} → custom_templates.yaml — Inspect a custom template
PUT /api/custom_templates/{id} → custom_templates.yaml — Update a template
DELETE /api/custom_templates/{id} → custom_templates.yaml — Remove a template
GET /api/custom_templates/{id}/file → custom_templates.yaml — Get Template stack file content.
PUT /api/custom_templates/{id}/git_fetch → custom_templates.yaml — Fetch the latest config file content based on custom template's git repository configuration
POST /api/docker/{environmentId}/containers/image_status/clear → docker.yaml — Clear container image status cache
GET /api/docker/{environmentId}/containers/{containerId}/gpus → docker.yaml — Fetch container gpus data
GET /api/docker/{environmentId}/containers/{containerId}/image_status → docker.yaml — Fetch image status for container
GET /api/docker/{environmentId}/dashboard → docker.yaml — Get counters for the dashboard
GET /api/docker/{environmentId}/images → docker.yaml — Fetch images
POST /api/docker/{environmentId}/services/image_status/clear → docker.yaml — Clear service image status cache
GET /api/docker/{environmentId}/services/{serviceId}/image_status → docker.yaml — Fetch image status for service
GET /api/docker/{environmentId}/snapshot → endpoints.yaml — Fetch latest snapshot of environment
GET /api/docker/{environmentId}/snapshot/containers → endpoints.yaml — Fetch containers list from a snapshot
GET /api/docker/{environmentId}/snapshot/containers/{containerId} → endpoints.yaml — Fetch container from a snapshot
GET /api/edge_configurations → edge_configs.yaml — List available Edge Configurations
POST /api/edge_configurations → edge_configs.yaml — Create an Edge Configuration
GET /api/edge_configurations/{id} → edge_configs.yaml — Inspect an Edge configuration
PUT /api/edge_configurations/{id} → edge_configs.yaml — Update an Edge Configuration
DELETE /api/edge_configurations/{id} → edge_configs.yaml — Delete an Edge configuration
GET /api/edge_configurations/{id}/files → edge_configs.yaml — Get the files for an Edge configuration
PUT /api/edge_configurations/{id}/{state} → edge_configs.yaml — Update the state of an Edge configuration
GET /api/edge_groups → edge_groups.yaml — list EdgeGroups
POST /api/edge_groups → edge_groups.yaml — Create an EdgeGroup
GET /api/edge_groups/{id} → edge_groups.yaml — Inspects an EdgeGroup
PUT /api/edge_groups/{id} → edge_groups.yaml — Updates an EdgeGroup
DELETE /api/edge_groups/{id} → edge_groups.yaml — Deletes an EdgeGroup
GET /api/edge_jobs → edge_jobs.yaml — Fetch EdgeJobs list
POST /api/edge_jobs/create/file → edge_jobs.yaml — Create an EdgeJob from a file
POST /api/edge_jobs/create/string → edge_jobs.yaml — Create an EdgeJob from a text
GET /api/edge_jobs/{id} → edge_jobs.yaml — Inspect an EdgeJob
PUT /api/edge_jobs/{id} → edge_jobs.yaml — Update an EdgeJob
DELETE /api/edge_jobs/{id} → edge_jobs.yaml — Delete an EdgeJob
GET /api/edge_jobs/{id}/file → edge_jobs.yaml — Fetch a file of an EdgeJob
GET /api/edge_jobs/{id}/tasks → edge_jobs.yaml — Fetch the list of tasks on an EdgeJob
GET /api/edge_jobs/{id}/tasks/{taskID}/logs → edge_jobs.yaml — Fetch the log for a specifc task on an EdgeJob
POST /api/edge_jobs/{id}/tasks/{taskID}/logs → edge_jobs.yaml — Collect the log for a specifc task on an EdgeJob
DELETE /api/edge_jobs/{id}/tasks/{taskID}/logs → edge_jobs.yaml — Clear the log for a specifc task on an EdgeJob
GET /api/edge_stacks → edge_stacks.yaml — Fetches the list of EdgeStacks
POST /api/edge_stacks/create/file → edge_stacks.yaml — Create an EdgeStack from file
POST /api/edge_stacks/create/helmRepo → edge_stacks.yaml — Create an EdgeStack from a Helm repository
POST /api/edge_stacks/create/repository → edge_stacks.yaml — Create an EdgeStack from a git repository
POST /api/edge_stacks/create/string → edge_stacks.yaml — Create an EdgeStack from a text
POST /api/edge_stacks/parse_registries → edge_stacks.yaml — Parse registries from a stack file
POST /api/edge_stacks/webhooks/{webhookID} → stacks.yaml — Webhook for triggering edge stack updates from git
GET /api/edge_stacks/{id} → edge_stacks.yaml — Inspect an EdgeStack
PUT /api/edge_stacks/{id} → edge_stacks.yaml — Update an EdgeStack
DELETE /api/edge_stacks/{id} → edge_stacks.yaml — Delete an EdgeStack
GET /api/edge_stacks/{id}/file → edge_stacks.yaml — Fetches the stack file for an EdgeStack
PUT /api/edge_stacks/{id}/git → edge_stacks.yaml — Update git configuration and pulls the repository
PUT /api/edge_stacks/{id}/helmRepo → edge_stacks.yaml — Update an EdgeStack from a Helm repository
GET /api/edge_stacks/{id}/logs/{endpoint_id} → edge_stacks.yaml — Gets the status of the log collection for a given edgestack and environment
PUT /api/edge_stacks/{id}/logs/{endpoint_id} → edge_stacks.yaml — Schedule the collection of logs for a given endpoint and edge stack
DELETE /api/edge_stacks/{id}/logs/{endpoint_id} → edge_stacks.yaml — Deletes the available logs for a given edge stack and endpoint
GET /api/edge_stacks/{id}/logs/{endpoint_id}/file → edge_stacks.yaml — Downloads the available logs for a given edge stack and endpoint
GET /api/edge_stacks/{id}/stagger/status → edge_stacks.yaml — Inspect an EdgeStack's parallel update status
PUT /api/edge_stacks/{id}/status → edge_stacks.yaml — Update an EdgeStack status
GET /api/edge_update_schedules → edge_update_schedules.yaml — Fetches the list of Edge Update Schedules
POST /api/edge_update_schedules → edge_update_schedules.yaml — Creates a scheduled remote update procedure for Edge agents
POST /api/edge_update_schedules/active → edge_update_schedules.yaml — Fetches the list of Active Edge Update Schedules
GET /api/edge_update_schedules/agent_versions → edge_update_schedules.yaml — Fetches the supported versions of the agent to update/rollback
GET /api/edge_update_schedules/info → edge_update_schedules.yaml — Returns informations the help create edge update schedules
GET /api/edge_update_schedules/previous_versions → edge_update_schedules.yaml — Fetches the previous versions of updated agents
GET /api/edge_update_schedules/{id} → edge_update_schedules.yaml — Returns the Edge Update Schedule with the given ID
POST /api/edge_update_schedules/{id} → edge_update_schedules.yaml — Update a scheduled remote update procedure for Edge agents
DELETE /api/edge_update_schedules/{id} → edge_update_schedules.yaml — Deletes an Edge Update Schedule
GET /api/endpoint_groups → endpoint_groups.yaml — List Environment(Endpoint) groups
POST /api/endpoint_groups → endpoint_groups.yaml — Create an Environment(Endpoint) Group
GET /api/endpoint_groups/{id} → endpoint_groups.yaml — Inspect an Environment(Endpoint) group
PUT /api/endpoint_groups/{id} → endpoint_groups.yaml — Update an environment(endpoint) group
DELETE /api/endpoint_groups/{id} → endpoint_groups.yaml — Remove an environment(endpoint) group
PUT /api/endpoint_groups/{id}/endpoints/{endpointId} → endpoint_groups.yaml — Add an environment(endpoint) to an environment(endpoint) group
DELETE /api/endpoint_groups/{id}/endpoints/{endpointId} → endpoint_groups.yaml — Removes environment(endpoint) from an environment(endpoint) group
GET /api/endpoints → endpoints.yaml — List environments(endpoints)
POST /api/endpoints → endpoints.yaml — Create a new environment(endpoint)
DELETE /api/endpoints → endpoints.yaml — Remove multiple environments
GET /api/endpoints/agent_versions → endpoints.yaml — List agent versions
POST /api/endpoints/delete → endpoints.yaml — Remove multiple environments
POST /api/endpoints/edge/async → endpoints.yaml — Get environment(endpoint) status
POST /api/endpoints/edge/generate-key → edge.yaml — Generate an EdgeKey
POST /api/endpoints/edge/trust → endpoints.yaml — Associate one or more Edge environments in the waiting room to environments
POST /api/endpoints/global-key → endpoints.yaml — Create or retrieve the endpoint for an EdgeID
PUT /api/endpoints/relations → endpoints.yaml — Update relations for a list of environments
POST /api/endpoints/snapshot → endpoints.yaml — Snapshot all environment(endpoint)
GET /api/endpoints/summary → endpoints.yaml — Get environment summary counts
GET /api/endpoints/{id} → endpoints.yaml — Inspect an environment(endpoint)
PUT /api/endpoints/{id} → endpoints.yaml — Update an environment(endpoint)
DELETE /api/endpoints/{id} → endpoints.yaml — Remove an environment
PUT /api/endpoints/{id}/association → endpoints.yaml — De-association an edge environment(endpoint)
POST /api/endpoints/{id}/docker/v2/browse/put → endpoints.yaml — Upload a file under a specific path on the file system of an environment (endpoint)
GET /api/endpoints/{id}/dockerhub/{registryId} → endpoints.yaml — fetch docker pull limits
POST /api/endpoints/{id}/edge/alerts → edge.yaml — Receive Alertmanager-compatible alerts from an Edge agent
GET /api/endpoints/{id}/edge/alerts/state → endpoints.yaml — Get edge agent alert evaluation state
GET /api/endpoints/{id}/edge/charts → endpoints.yaml — Get environment(endpoint) charts to install
PUT /api/endpoints/{id}/edge/charts/statuses → endpoints.yaml — Update environment(endpoint) policy chart installation statuses
POST /api/endpoints/{id}/edge/jobs/{jobID}/logs → edge.yaml — Update the logs collected from an Edge Job
GET /api/endpoints/{id}/edge/stacks/{stackId} → edge.yaml — Inspect an Edge Stack for an Environment(Endpoint)
GET /api/endpoints/{id}/edge/status → endpoints.yaml — Get environment(endpoint) status
PUT /api/endpoints/{id}/forceupdateservice → endpoints.yaml — force update a docker service
GET /api/endpoints/{id}/kubernetes/helm → helm.yaml — List Helm Releases
POST /api/endpoints/{id}/kubernetes/helm → helm.yaml — Install Helm Chart
POST /api/endpoints/{id}/kubernetes/helm/git/dryrun → helm.yaml — Helm Git Dry Run
GET /api/endpoints/{id}/kubernetes/helm/{release} → helm.yaml — Get a helm release
DELETE /api/endpoints/{id}/kubernetes/helm/{release} → helm.yaml — Delete Helm Release
GET /api/endpoints/{id}/kubernetes/helm/{release}/history → helm.yaml — Get a historical list of releases
POST /api/endpoints/{id}/kubernetes/helm/{release}/rollback → helm.yaml — Rollback a helm release
GET /api/endpoints/{id}/mtls_certificate → endpoints.yaml — Get an environment(endpoint) mTLS certificate
GET /api/endpoints/{id}/mtls_certificate_error → endpoints.yaml — Get an agent(endpoint) mTLS certificate
PUT /api/endpoints/{id}/pools/{rpn}/access → endpoints.yaml — Update namespace access for a given namespace
GET /api/endpoints/{id}/registries → endpoints.yaml — List Registries on environment
PUT /api/endpoints/{id}/registries/{registryId} → endpoints.yaml — update registry access for environment
PUT /api/endpoints/{id}/settings → endpoints.yaml — Update settings for an environment(endpoint)
POST /api/endpoints/{id}/snapshot → endpoints.yaml — Snapshots an environment(endpoint)
POST /api/gitops/repo/file/preview → gitops.yaml — preview the content of target file in the git repository
POST /api/gitops/repo/files/search → gitops.yaml — Search the file path from a git repository files with specified extensions
POST /api/gitops/repo/helm/values → gitops.yaml — Preview merged Helm values files from git repository
POST /api/gitops/repo/refs → gitops.yaml — List the refs of a git repository
GET /api/gitops/sources → gitops.yaml — List all GitOps sources
GET /api/gitops/sources/summary → gitops.yaml — Summarize GitOps source status counts
GET /api/gitops/sources/{id} → gitops.yaml — Get a GitOps source by ID
GET /api/gitops/workflows → gitops.yaml — List all GitOps workflows
GET /api/gitops/workflows/summary → gitops.yaml — Summarize GitOps workflow status counts
GET /api/kubernetes/{environmentId}/opa → kubernetes.yaml — Get Pod Security Rule within k8s cluster, if not found, the frontend will create a default
PUT /api/kubernetes/{environmentId}/opa → kubernetes.yaml — Update Pod Security Rule within k8s cluster
GET /api/kubernetes/{id}/applications → kubernetes.yaml — Get a list of applications across all namespaces in the cluster. If the nodeName is provided, it will return the applications running on that node.
GET /api/kubernetes/{id}/applications/count → kubernetes.yaml — Get Applications count
POST /api/kubernetes/{id}/cluster_role_bindings/delete → kubernetes.yaml — Delete cluster role bindings
POST /api/kubernetes/{id}/cluster_roles/delete → kubernetes.yaml — Delete cluster roles
GET /api/kubernetes/{id}/clusterrolebindings → kubernetes.yaml — Get a list of kubernetes cluster role bindings
GET /api/kubernetes/{id}/clusterroles → kubernetes.yaml — Get a list of kubernetes cluster roles
GET /api/kubernetes/{id}/configmaps → kubernetes.yaml — Get a list of ConfigMaps
GET /api/kubernetes/{id}/configmaps/count → kubernetes.yaml — Get ConfigMaps count
GET /api/kubernetes/{id}/cron_jobs → kubernetes.yaml — Get a list of kubernetes Cron Jobs
POST /api/kubernetes/{id}/cron_jobs/delete → kubernetes.yaml — Delete Cron Jobs
GET /api/kubernetes/{id}/customresourcedefinitions → kubernetes.yaml — Get a list of kubernetes Custom Resource Definitions
GET /api/kubernetes/{id}/customresourcedefinitions/{name} → kubernetes.yaml — Get a kubernetes Custom Resource Definition
DELETE /api/kubernetes/{id}/customresourcedefinitions/{name} → kubernetes.yaml — Delete a kubernetes Custom Resource Definition
GET /api/kubernetes/{id}/customresources → kubernetes.yaml — Get a list of kubernetes Custom Resources
GET /api/kubernetes/{id}/customresources/{namespace}/{name} → kubernetes.yaml — Get a list of kubernetes Custom Resources
DELETE /api/kubernetes/{id}/customresources/{namespace}/{name} → kubernetes.yaml — Delete a namespaced kubernetes Custom Resource
GET /api/kubernetes/{id}/customresources/{name} → kubernetes.yaml — Get a cluster-scoped kubernetes Custom Resource
DELETE /api/kubernetes/{id}/customresources/{name} → kubernetes.yaml — Delete a cluster-scoped kubernetes Custom Resource
GET /api/kubernetes/{id}/dashboard → kubernetes.yaml — Get the dashboard summary data
GET /api/kubernetes/{id}/describe → kubernetes.yaml — Get a description of a kubernetes resource
GET /api/kubernetes/{id}/events → kubernetes.yaml — Gets kubernetes events
GET /api/kubernetes/{id}/ingresscontrollers → kubernetes.yaml — Get a list of ingress controllers
PUT /api/kubernetes/{id}/ingresscontrollers → kubernetes.yaml — Update (block/unblock) ingress controllers
GET /api/kubernetes/{id}/ingresses → kubernetes.yaml — Get kubernetes ingresses at the cluster level
GET /api/kubernetes/{id}/ingresses/count → kubernetes.yaml — Get Ingresses count
POST /api/kubernetes/{id}/ingresses/delete → kubernetes.yaml — Delete one or more Ingresses
GET /api/kubernetes/{id}/jobs → kubernetes.yaml — Get a list of kubernetes Jobs
POST /api/kubernetes/{id}/jobs/delete → kubernetes.yaml — Delete Jobs
GET /api/kubernetes/{id}/max_resource_limits → kubernetes.yaml — Get max CPU and memory limits of all nodes within k8s cluster
GET /api/kubernetes/{id}/metrics/applications_resources → kubernetes.yaml — Get the total CPU (cores) and memory requests (MB) and limits of all applications across all namespaces
GET /api/kubernetes/{id}/metrics/nodes → kubernetes.yaml — Get a list of nodes with their live metrics
GET /api/kubernetes/{id}/metrics/nodes/{name} → kubernetes.yaml — Get live metrics for a node
GET /api/kubernetes/{id}/metrics/pods/{namespace} → kubernetes.yaml — Get a list of pods with their live metrics
GET /api/kubernetes/{id}/metrics/pods/{namespace}/{name} → kubernetes.yaml — Get live metrics for a pod
GET /api/kubernetes/{id}/namespaces → kubernetes.yaml — Get a list of namespaces
PUT /api/kubernetes/{id}/namespaces → kubernetes.yaml — Update a namespace
POST /api/kubernetes/{id}/namespaces → kubernetes.yaml — Create a namespace
DELETE /api/kubernetes/{id}/namespaces → kubernetes.yaml — Delete a kubernetes namespace
GET /api/kubernetes/{id}/namespaces/count → kubernetes.yaml — Get the total number of kubernetes namespaces within the given Portainer environment.
GET /api/kubernetes/{id}/namespaces/{namespace} → kubernetes.yaml — Get namespace details
PUT /api/kubernetes/{id}/namespaces/{namespace} → kubernetes.yaml — Update a namespace
POST /api/kubernetes/{id}/namespaces/{namespace}/applications/{kind}/{name}/restart → kubernetes.yaml — Restart a Kubernetes application
GET /api/kubernetes/{id}/namespaces/{namespace}/applications/{name} → kubernetes.yaml — Get an application by name in a specific namespace
GET /api/kubernetes/{id}/namespaces/{namespace}/configmaps/{configmap} → kubernetes.yaml — Get a ConfigMap
GET /api/kubernetes/{id}/namespaces/{namespace}/events → kubernetes.yaml — Gets kubernetes events for namespace
GET /api/kubernetes/{id}/namespaces/{namespace}/ingresscontrollers → kubernetes.yaml — Get a list ingress controllers by namespace
PUT /api/kubernetes/{id}/namespaces/{namespace}/ingresscontrollers → kubernetes.yaml — Update (block/unblock) ingress controllers by namespace
GET /api/kubernetes/{id}/namespaces/{namespace}/ingresses → kubernetes.yaml — Get a list of Ingresses
PUT /api/kubernetes/{id}/namespaces/{namespace}/ingresses → kubernetes.yaml — Update an Ingress
POST /api/kubernetes/{id}/namespaces/{namespace}/ingresses → kubernetes.yaml — Create an Ingress
GET /api/kubernetes/{id}/namespaces/{namespace}/ingresses/{ingress} → kubernetes.yaml — Get an Ingress by name
GET /api/kubernetes/{id}/namespaces/{namespace}/persistent_volume_claims → kubernetes.yaml — Get PersistentVolumeClaims in a namespace
GET /api/kubernetes/{id}/namespaces/{namespace}/persistent_volume_claims/{name} → kubernetes.yaml — Get a specific PersistentVolumeClaim
DELETE /api/kubernetes/{id}/namespaces/{namespace}/pods/{name} → kubernetes.yaml — Delete a kubernetes pod
POST /api/kubernetes/{id}/namespaces/{namespace}/pods/{name}/restart → kubernetes.yaml — Restart all containers in a Kubernetes pod
GET /api/kubernetes/{id}/namespaces/{namespace}/secrets/{secret} → kubernetes.yaml — Get a Secret
GET /api/kubernetes/{id}/namespaces/{namespace}/service_accounts/{name} → kubernetes.yaml — Get a kubernetes service account
PUT /api/kubernetes/{id}/namespaces/{namespace}/service_accounts/{name}/image_pull_secrets → kubernetes.yaml — Update image pull secrets for a service account
GET /api/kubernetes/{id}/namespaces/{namespace}/services → kubernetes.yaml — Get a list of services for a given namespace
PUT /api/kubernetes/{id}/namespaces/{namespace}/services → kubernetes.yaml — Update a service
POST /api/kubernetes/{id}/namespaces/{namespace}/services → kubernetes.yaml — Create a service
PUT /api/kubernetes/{id}/namespaces/{namespace}/system → kubernetes.yaml — Toggle the system state for a namespace
GET /api/kubernetes/{id}/namespaces/{namespace}/volumes → kubernetes.yaml — Get Kubernetes volumes within a namespace in the given Portainer environment
POST /api/kubernetes/{id}/nodes/{name}/drain → kubernetes.yaml — Drain a Kubernetes node
GET /api/kubernetes/{id}/nodes_limits → kubernetes.yaml — Get CPU and memory limits of all nodes within k8s cluster.
GET /api/kubernetes/{id}/persistent_volume_claims → kubernetes.yaml — Get all PersistentVolumeClaims
POST /api/kubernetes/{id}/persistent_volume_claims/delete → kubernetes.yaml — Delete PersistentVolumeClaims
PUT /api/kubernetes/{id}/persistent_volume_claims/resize → kubernetes.yaml — Resize a PersistentVolumeClaim
GET /api/kubernetes/{id}/persistent_volumes → kubernetes.yaml — Get all PersistentVolumes in the cluster
POST /api/kubernetes/{id}/persistent_volumes/delete → kubernetes.yaml — Delete PersistentVolumes
PUT /api/kubernetes/{id}/persistent_volumes/reclaim_policy → kubernetes.yaml — Update reclaim policy of a PersistentVolume
GET /api/kubernetes/{id}/persistent_volumes/{name} → kubernetes.yaml — Get a specific PersistentVolume
GET /api/kubernetes/{id}/rbac_enabled → kubernetes.yaml — Check if RBAC is enabled
POST /api/kubernetes/{id}/role_bindings/delete → kubernetes.yaml — Delete role bindings
GET /api/kubernetes/{id}/rolebindings → kubernetes.yaml — Get a list of kubernetes role bindings
GET /api/kubernetes/{id}/roles → kubernetes.yaml — Get a list of kubernetes roles
POST /api/kubernetes/{id}/roles/delete → kubernetes.yaml — Delete roles
GET /api/kubernetes/{id}/secrets → kubernetes.yaml — Get a list of Secrets
GET /api/kubernetes/{id}/secrets/count → kubernetes.yaml — Get Secrets count
POST /api/kubernetes/{id}/service_accounts/delete → kubernetes.yaml — Delete service accounts
GET /api/kubernetes/{id}/serviceaccounts → kubernetes.yaml — Get a list of kubernetes service accounts
GET /api/kubernetes/{id}/services → kubernetes.yaml — Get a list of services
GET /api/kubernetes/{id}/services/count → kubernetes.yaml — Get services count
POST /api/kubernetes/{id}/services/delete → kubernetes.yaml — Delete services
GET /api/kubernetes/{id}/storage_classes → kubernetes.yaml — Get all StorageClasses
POST /api/kubernetes/{id}/storage_classes/delete → kubernetes.yaml — Delete StorageClasses
GET /api/kubernetes/{id}/storage_classes/{name} → kubernetes.yaml — Get a specific StorageClass
PUT /api/kubernetes/{id}/storage_classes/{name}/default → kubernetes.yaml — Set a StorageClass as default
GET /api/kubernetes/{id}/version → kubernetes.yaml — Get the Kubernetes cluster version and Portainer-relevant capabilities
GET /api/kubernetes/{id}/volumes → kubernetes.yaml — Get Kubernetes volumes within the given Portainer environment
GET /api/kubernetes/{id}/volumes/count → kubernetes.yaml — Get the total number of kubernetes volumes within the given Portainer environment.
GET /api/kubernetes/{id}/volumes/{namespace}/{volume} → kubernetes.yaml — Get a Kubernetes volume within the given Portainer environment
POST /api/ldap/admin-groups → ldap.yaml — Fetch LDAP admin groups
POST /api/ldap/check → ldap.yaml — Test LDAP connectivity
POST /api/ldap/groups → ldap.yaml — Search LDAP Groups
POST /api/ldap/test → ldap.yaml — Test Login to ldap server
POST /api/ldap/users → ldap.yaml — Search LDAP Users
GET /api/licenses → license.yaml — fetches the list of licenses on Portainer
POST /api/licenses/add → license.yaml — attaches a list of licenses to Portainer
GET /api/licenses/info → license.yaml — summarizes licenses on Portainer
POST /api/licenses/remove → license.yaml — delete license from portainer instance
GET /api/motd → motd.yaml — fetches the message of the day
GET /api/observability/alerting/alerts → observability.yaml — Get active or silenced alerts
GET /api/observability/alerting/connectivity → observability.yaml — Test AlertManager connectivity
GET /api/observability/alerting/rules → observability.yaml — Get all alert rules
POST /api/observability/alerting/rules → observability.yaml — Create an alert rule
GET /api/observability/alerting/rules/{id} → observability.yaml — Get an alert rule by ID
PUT /api/observability/alerting/rules/{id} → observability.yaml — Update an alert rule
DELETE /api/observability/alerting/rules/{id} → observability.yaml — Delete an alert rule
GET /api/observability/alerting/rules/{id}/environments → observability.yaml — Get alert rule environments
PUT /api/observability/alerting/rules/{id}/groups → observability.yaml — Update alert rule endpoint groups
PUT /api/observability/alerting/rules/{id}/tiers → observability.yaml — Update alert rule tiers
GET /api/observability/alerting/settings → observability.yaml — Get all alerting settings
PUT /api/observability/alerting/settings → observability.yaml — Update alerting settings
GET /api/observability/alerting/settings/{id} → observability.yaml — Get alerting settings by ID
POST /api/observability/alerting/silence → observability.yaml — Create an alert silence
DELETE /api/observability/alerting/silence/{id} → observability.yaml — Delete an alert silence
GET /api/omni/serviceaccount/validate → omni.yaml — Validate an Omni service account
POST /api/omni/{credentialID}/cluster/create → omni.yaml — Create an Omni cluster
DELETE /api/omni/{credentialID}/cluster/delete → omni.yaml — Delete an Omni cluster
PUT /api/omni/{credentialID}/cluster/update → omni.yaml — Update an Omni cluster
GET /api/omni/{credentialID}/cluster/version/talos → omni.yaml — Get supported Talos versions
GET /api/omni/{credentialID}/cluster/{name} → omni.yaml — Get an Omni cluster
GET /api/omni/{credentialID}/cluster/{name}/upgrade/status/k8s → omni.yaml — Get Kubernetes upgrade status for an Omni cluster
GET /api/omni/{credentialID}/cluster/{name}/upgrade/status/talos → omni.yaml — Get Talos upgrade status for an Omni cluster
GET /api/omni/{credentialID}/machine/{name} → omni.yaml — Get an Omni machine
GET /api/omni/{credentialID}/machine/{name}/logs → omni.yaml — Get logs for an Omni machine
GET /api/omni/{credentialID}/machines → omni.yaml — List Omni machines
POST /api/omni/{credentialID}/node/{name} → omni.yaml — Reboot an Omni node
GET /api/policies → policies.yaml — List policies
POST /api/policies → policies.yaml — Create a new policy
POST /api/policies/conflicts → policies.yaml — Get policy conflicts preview
GET /api/policies/metadata → policies.yaml — Get policy metadata
GET /api/policies/templates → policies.yaml — List policy templates
GET /api/policies/templates/{id} → policies.yaml — Inspect a policy template
GET /api/policies/{id} → policies.yaml — Inspect a policy
PUT /api/policies/{id} → policies.yaml — Update a policy
DELETE /api/policies/{id} → policies.yaml — Delete a policy
GET /api/recommendations → recommendations.yaml — List recommendations
GET /api/recommendations/summary → recommendations.yaml — Get recommendation summary
GET /api/registries → registries.yaml — List Registries
POST /api/registries → registries.yaml — Create a new registry
POST /api/registries/ping → registries.yaml — Test registry connection
GET /api/registries/{id} → registries.yaml — Inspect a registry
PUT /api/registries/{id} → registries.yaml — Update a registry
DELETE /api/registries/{id} → registries.yaml — Remove a registry
POST /api/registries/{id}/configure → registries.yaml — Configures a registry
DELETE /api/registries/{id}/ecr/repositories/{repositoryName} → registries.yaml — Delete ECR repository
DELETE /api/registries/{id}/ecr/repositories/{repositoryName}/tags → registries.yaml — Delete tags
DELETE /api/registries/{id}/repositories/{repositoryName}/tags → registries.yaml — Delete repository tags
POST /api/resource_controls → resource_controls.yaml — Create a new resource control
PUT /api/resource_controls/{id} → resource_controls.yaml — Update a resource control
DELETE /api/resource_controls/{id} → resource_controls.yaml — Remove a resource control
POST /api/restore → backup.yaml — Triggers a system restore using provided backup file
GET /api/roles → roles.yaml — List roles
GET /api/settings → settings.yaml — Retrieve Portainer settings
PUT /api/settings → settings.yaml — Update Portainer settings
GET /api/settings/additional_functionality → settings.yaml — Retrieve Portainer additional functionality settings
PUT /api/settings/additional_functionality → settings.yaml — Update Portainer additional functionality settings
PUT /api/settings/default_registry → settings.yaml — Update Portainer default registry settings
GET /api/settings/edge/mtls_ca_certificate → settings.yaml — Retrieve Portainer Edge MTLS CA Certificates
GET /api/settings/edge/mtls_certificate → settings.yaml — Retrieve Portainer Edge MTLS Certificates
GET /api/settings/experimental → settings.yaml — Retrieve Portainer experimental settings
PUT /api/settings/experimental → settings.yaml — Update Portainer experimental settings
GET /api/settings/public → settings.yaml — Retrieve Portainer public settings
POST /api/sshkeygen → cloud_credentials.yaml — Generate ssh keypair
GET /api/ssl → ssl.yaml — Inspect the ssl settings
PUT /api/ssl → ssl.yaml — Update the ssl settings
GET /api/stacks → stacks.yaml — List stacks
POST /api/stacks/create/kubernetes/repository → stacks.yaml — Deploy a new kubernetes stack from a git repository
POST /api/stacks/create/kubernetes/string → stacks.yaml — Deploy a new kubernetes stack from a file
POST /api/stacks/create/kubernetes/url → stacks.yaml — Deploy a new kubernetes stack from a url
POST /api/stacks/create/standalone/file → stacks.yaml — Deploy a new compose stack from a file
POST /api/stacks/create/standalone/repository → stacks.yaml — Deploy a new compose stack from repository
POST /api/stacks/create/standalone/string → stacks.yaml — Deploy a new compose stack from a text
POST /api/stacks/create/swarm/file → stacks.yaml — Deploy a new swarm stack from a file
POST /api/stacks/create/swarm/repository → stacks.yaml — Deploy a new swarm stack from a git repository
POST /api/stacks/create/swarm/string → stacks.yaml — Deploy a new swarm stack from a text
POST /api/stacks/image_status/clear → docker.yaml — Clear stack image status cache
DELETE /api/stacks/name/{name} → stacks.yaml — Remove Kubernetes stacks by name
POST /api/stacks/webhooks/{webhookID} → stacks.yaml — Webhook for triggering stack updates from git
GET /api/stacks/{id} → stacks.yaml — Inspect a stack
PUT /api/stacks/{id} → stacks.yaml — Update a stack
DELETE /api/stacks/{id} → stacks.yaml — Remove a stack
PUT /api/stacks/{id}/associate → stacks.yaml — Associate an orphaned stack to a new environment(endpoint)
POST /api/stacks/{id}/convert → stacks.yaml — Convert a Docker Compose stack to Kubernetes manifests or Helm chart
GET /api/stacks/{id}/file → stacks.yaml — Retrieve the content of the Stack file for the specified stack
POST /api/stacks/{id}/git → stacks.yaml — Update a stack's Git configs
PUT /api/stacks/{id}/git/redeploy → stacks.yaml — Redeploy a stack
GET /api/stacks/{id}/images_status → stacks.yaml — Fetch image status for stack
POST /api/stacks/{id}/migrate → stacks.yaml — Migrate a stack to another environment(endpoint)
POST /api/stacks/{id}/start → stacks.yaml — Starts a stopped Stack
POST /api/stacks/{id}/stop → stacks.yaml — Stops a stopped Stack
GET /api/status → status.yaml — Check Portainer status
GET /api/support/debug_log → support.yaml — Get status of the global debug log
PUT /api/support/debug_log → support.yaml — Enables or disables the global debug log
POST /api/support/download → support.yaml — Download a bundle of files to help support diagnose issues
GET /api/system/info → system.yaml — Retrieve system info
GET /api/system/nodes → system.yaml — Retrieve the count of nodes
GET /api/system/status → system.yaml — Check Portainer status
POST /api/system/update → system.yaml — Update Portainer to latest version
GET /api/system/version → system.yaml — Check for portainer updates
GET /api/tags → tags.yaml — List tags
POST /api/tags → tags.yaml — Create a new tag
DELETE /api/tags/{id} → tags.yaml — Remove a tag
GET /api/team_memberships → team_memberships.yaml — List team memberships
POST /api/team_memberships → team_memberships.yaml — Create a new team membership
PUT /api/team_memberships/{id} → team_memberships.yaml — Update a team membership
DELETE /api/team_memberships/{id} → team_memberships.yaml — Remove a team membership
GET /api/teams → teams.yaml — List teams
POST /api/teams → teams.yaml — Create a new team
GET /api/teams/{id} → teams.yaml — Inspect a team
PUT /api/teams/{id} → teams.yaml — Update a team
DELETE /api/teams/{id} → teams.yaml — Remove a team
GET /api/teams/{id}/memberships → team_memberships.yaml — List team memberships
GET /api/templates → templates.yaml — List available templates
GET /api/templates/helm → helm.yaml — Search Helm Charts
GET /api/templates/helm/{command} → helm.yaml — Show Helm Chart Information
POST /api/templates/{id}/file → templates.yaml — Get a template's file
POST /api/upload/tls/{certificate} → upload.yaml — Upload TLS files
GET /api/useractivity/authlogs → useractivity.yaml — List auth activity logs
GET /api/useractivity/authlogs.csv → useractivity.yaml — Download auth logs as CSV
GET /api/useractivity/logs → useractivity.yaml — List user activity logs
GET /api/useractivity/logs.csv → useractivity.yaml — Download user activity logs as CSV
GET /api/users → users.yaml — List users
POST /api/users → users.yaml — Create a new user
GET /api/users/admin/check → users.yaml — Check administrator account existence
POST /api/users/admin/init → users.yaml — Initialize administrator account
GET /api/users/me → users.yaml — Inspect the current user
GET /api/users/me/auth/{endpointID} → users.yaml — Inspect environment authorizations for the current user
GET /api/users/{id} → users.yaml — Inspect a user
PUT /api/users/{id} → users.yaml — Update a user
DELETE /api/users/{id} → users.yaml — Remove a user
GET /api/users/{id}/effective-access → users.yaml — Inspect a user's effective access on every environment
GET /api/users/{id}/gitcredentials → users.yaml — Get all saved git credentials for a user
POST /api/users/{id}/gitcredentials → users.yaml — Store a Git Credential for a user
GET /api/users/{id}/gitcredentials/{credentialID} → users.yaml — Get the specific saved git credential for a user
PUT /api/users/{id}/gitcredentials/{credentialID} → users.yaml — Update a git-credential associated to a user
DELETE /api/users/{id}/gitcredentials/{credentialID} → users.yaml — Remove a git-credential associated to a user
GET /api/users/{id}/helm/repositories → helm.yaml — List a users helm repositories
POST /api/users/{id}/helm/repositories → helm.yaml — Create a user helm repository
DELETE /api/users/{id}/helm/repositories/{repositoryID} → helm.yaml — Delete a users helm repository
GET /api/users/{id}/memberships → users.yaml — Inspect a user memberships
GET /api/users/{id}/namespaces → users.yaml — Retrieves all k8s namespaces for an user
PUT /api/users/{id}/passwd → users.yaml — Update password for a user
GET /api/users/{id}/tokens → users.yaml — Get all API keys for a user
POST /api/users/{id}/tokens → users.yaml — Generate an API key for a user
DELETE /api/users/{id}/tokens/{keyID} → users.yaml — Remove an api-key associated to a user
GET /api/webhooks → webhooks.yaml — List webhooks
POST /api/webhooks → webhooks.yaml — Create a webhook
PUT /api/webhooks/{id} → webhooks.yaml — Update a webhook
POST /api/webhooks/{id} → webhooks.yaml — Execute a webhook
DELETE /api/webhooks/{id} → webhooks.yaml — Delete a webhook
PUT /api/webhooks/{id}/reassign → webhooks.yaml — Reassign a webhook to another resource
GET /api/websocket/attach → websocket.yaml — Attach a websocket
GET /api/websocket/exec → websocket.yaml — Execute a websocket
GET /api/websocket/kubernetes-shell → websocket.yaml — Execute a websocket on kubectl shell pod
GET /api/websocket/pod → websocket.yaml — Execute a websocket on pod
